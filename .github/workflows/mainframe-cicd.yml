# Intentionally inactive — CI/CD is Jenkins + Zowe only (see Jenkinsfile).
# No push trigger; kept so the workflows directory remains valid if re-enabled later.

name: Mainframe CI/CD (inactive)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "Use Jenkins at localhost:8080 — this workflow does not deploy to z/OS."
